import 'package:flutter/widgets.dart';

class BigContainerOfRegister extends StatelessWidget {
  const BigContainerOfRegister({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
